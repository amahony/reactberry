throw new Error(
  '@reactberry/forms no longer exposes the legacy `index.js -> require("./dist")` shell. The package-root contract is being rebuilt for the hard-break release; do not rely on `/dist` or `/src` deep imports.'
);
