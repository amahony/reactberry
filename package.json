{
  "name": "root",
  "private": true,
  "useWorkspaces": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
   "node": ">=12.0.0"
 },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "clean": "lerna run clean",
    "dev": "cd packages/playground && yarn start"
  },
  "devDependencies": {
    "lerna": "^3.15.0"
  }
}
